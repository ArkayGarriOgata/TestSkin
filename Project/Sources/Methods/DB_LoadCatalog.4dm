//%attributes = {}
// (PM) DB_LoadCatalog

C_LONGINT:C283($vl_Rowset)
ARRAY TEXT:C222(at_TableNames; 0)

If (conn_id#0)
	// Get an array of all table names
	//$vl_Rowset:=MySQL Select (conn_id;"SHOW TABLES")
	//MySQL Column To Array ($vl_Rowset;"";1;at_TableNames)
	//MySQL Delete Row Set ($vl_Rowset)
	//MySQL Get Version (conn_id;pluginVersion;clientVersion;serverVersion)
	
End if 